build:
  docker:
    doctory: doctory/Dockerfile
    app: app/Dockerfile
    #web: nginx
run:
  doctory: python manage.py runserver 0.0.0.0:8000
  app: yarn build
  # web: nginx start
release:
  image: doctory
  command: 
    - chmod u+x ./release.sh && ./release.sh